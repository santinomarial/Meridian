# Production Compose for Meridian (single API replica + Caddy TLS edge).
#
# Public VPS (recommended):
#   1. Copy .env.production.example → .env and fill secrets + DOMAIN
#   2. Point DNS A/AAAA at the VPS
#   3. docker compose -f docker-compose.prod.yml up --build -d
#
# Only Caddy publishes 80/443. Postgres, Redis, API, and SPA stay on the
# internal Compose network. Prefer one API replica for Socket.IO simplicity.
# See docs/how-to/deploy-with-docker-compose.md.

services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: meridian
      POSTGRES_USER: meridian
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U meridian -d meridian"]
      interval: 5s
      timeout: 5s
      retries: 10
    # Never publish Postgres to the host / public internet.
    expose:
      - "5432"

  redis:
    image: redis:7
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    # Never publish Redis to the host / public internet.
    expose:
      - "6379"

  migrate:
    build:
      context: ./server
      dockerfile: Dockerfile
      target: migrate
    environment:
      DATABASE_URL: postgresql://meridian:${POSTGRES_PASSWORD}@postgres:5432/meridian?schema=public
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://meridian:${POSTGRES_PASSWORD}@postgres:5432/meridian?schema=public
      REDIS_URL: redis://redis:6379
      REDIS_REQUIRED: "true"
      REDIS_KEY_PREFIX: ${REDIS_KEY_PREFIX:-prod:}
      JWT_SECRET: ${JWT_SECRET:?set JWT_SECRET}
      CLIENT_ORIGIN: ${CLIENT_ORIGIN:?set CLIENT_ORIGIN to https://YOUR_DOMAIN}
      TRUST_PROXY: "1"
      METRICS_ENABLED: "true"
      ENABLE_TERMINAL: "false"
      RESEND_API_KEY: ${RESEND_API_KEY:?set RESEND_API_KEY for account verification}
      MAIL_FROM: ${MAIL_FROM:?set MAIL_FROM to a sender on a verified domain}
      MAIL_TIMEOUT_MS: ${MAIL_TIMEOUT_MS:-10000}
      EMAIL_VERIFICATION_REQUIRED: "true"
      EMAIL_VERIFICATION_TTL_MINUTES: ${EMAIL_VERIFICATION_TTL_MINUTES:-1440}
    depends_on:
      migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    # Internal only — Caddy is the public edge. Do not map 3000 to the host.
    expose:
      - "3000"
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/ready').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  web:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        # Same-origin via Caddy: leave empty so the SPA uses window.location.origin.
        # Or set both to https://YOUR_DOMAIN explicitly.
        VITE_API_URL: ${VITE_API_URL:-}
        VITE_SOCKET_URL: ${VITE_SOCKET_URL:-}
        # Same-origin CSP ('self'). For a split API host, e.g.:
        # CSP_CONNECT_SRC_EXTRA= https://api.example.com wss://api.example.com
        CSP_CONNECT_SRC_EXTRA: ${CSP_CONNECT_SRC_EXTRA:-}
    restart: unless-stopped
    # Internal only — reached through Caddy.
    expose:
      - "8080"
    depends_on:
      api:
        condition: service_healthy

  caddy:
    image: caddy:2.9-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    environment:
      DOMAIN: ${DOMAIN:?set DOMAIN to your public hostname}
      ACME_EMAIL: ${ACME_EMAIL:?set ACME_EMAIL for Caddy ACME registration}
      API_UPSTREAMS: ${API_UPSTREAMS:-api:3000}
      LB_COOKIE_SECRET: ${LB_COOKIE_SECRET:?set LB_COOKIE_SECRET}
    volumes:
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      api:
        condition: service_healthy
      web:
        condition: service_started

  prometheus:
    image: ${PROMETHEUS_IMAGE:-prom/prometheus:v3.5.0}
    profiles: ["monitoring"]
    restart: unless-stopped
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=${PROMETHEUS_RETENTION:-15d}
    volumes:
      - ./deploy/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./deploy/monitoring/meridian-alerts.yml:/etc/prometheus/meridian-alerts.yml:ro
      - prometheus_data:/prometheus
    # Loopback only: inspect directly on the host or through an SSH tunnel.
    ports:
      - "127.0.0.1:9090:9090"
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres_data:
  redis_data:
  caddy_data:
  caddy_config:
  prometheus_data:
