# Local Docker Desktop testing only. Never use the development override publicly.
NODE_ENV=development
RUNNER_ALLOW_UNSAFE_LOCAL=true
PORT=4400
RUNNER_NAMESPACE=meridian-local
RUNNER_IMAGE=meridian-sandbox:local
# Generate with openssl rand -hex 32, and share only with the local API.
RUNNER_TOKEN=
