global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/meridian-alerts.yml

scrape_configs:
  - job_name: meridian-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - api:3000
