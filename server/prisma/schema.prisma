generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model — will be replaced when real domain models are added.
model Placeholder {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}
