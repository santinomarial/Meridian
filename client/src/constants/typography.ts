// Match CoderPad’s macOS editor, with native monospace fallbacks elsewhere.
// Keep Tailwind’s mono stack aligned so code labels use the same typography.
export const CODE_FONT_FAMILY = 'Menlo, Monaco, Consolas, "Courier New", monospace';
